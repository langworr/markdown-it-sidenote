.
Here is a sidenote reference,[$1] and another.[$longnote]

[$1]: Here is the sidenote.

[$longnote]: Here's one with multiple blocks.

    Subsequent paragraphs are indented to show that they
belong to the previous sidenote.

        { some.code }

    The whole paragraph can be indented, or just the first
    line.  In this way, multi-paragraph sidenotes work like
    multi-paragraph list items.

This paragraph won't be part of the note, because it
isn't indented.
.
<style>
    .sidenote {
    float: right;
    clear: right;
    width: 25%;
    margin: 0 0 0.5rem 1rem;
    box-sizing: border-box;
    background: #f9f9fb;
    border-left: 3px solid #c8c8d0;
    padding: 0.5rem 0.75rem;
    font-size: 0.9em;
    color: #333;
  }
</style><p>Here is a sidenote reference,<sup id="sidenote-test-doc-id-1">-test-doc-id-1</sup> and another.<sup id="sidenote-test-doc-id-2">-test-doc-id-2</sup></p>
<p>This paragraph won't be part of the note, because it
isn't indented.</p>
<!-- sidenote open -->
<span id="sidenote-test-doc-id-1" class="sidenote">
<p>Here is the sidenote.</p>
</span>
<span id="sidenote-test-doc-id-2" class="sidenote">
<p>Here's one with multiple blocks.</p>
<p>Subsequent paragraphs are indented to show that they
belong to the previous sidenote.</p>
<pre><code>{ some.code }
</code></pre>
<p>The whole paragraph can be indented, or just the first
line.  In this way, multi-paragraph sidenotes work like
multi-paragraph list items.</p>
</span>
<!-- sidenote close -->
.
